import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  product: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    margin: "0px 100px",
    paddingTop: "2rem",
  },
}));
